# Leave empty to use the Vite dev proxy (see vite.config.ts) -- requests to
# /api/* are forwarded to http://localhost:8000 automatically.
#
# Set this to your deployed backend's origin for production builds, e.g.
# VITE_API_BASE=https://webhookforge-api.onrender.com
VITE_API_BASE=
